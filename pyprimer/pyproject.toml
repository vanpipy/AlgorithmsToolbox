[project]
name = "pyprimer"
version = "0.1.0"
description = "Algorithms toolbox primer"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
